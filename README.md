# Королевство Флоптропика — Telegram RPG Bot

Рабочая модульная MVP-версия бота, адаптированная для Render.

## Что уже реализовано

- регистрация персонажа;
- загрузка ровно одного портрета персонажа через Telegram `file_id`;
- создание дома и загрузка ровно одного изображения дома;
- профиль персонажа;
- карточка дома;
- ежедневная тренировка;
- работа на 2 часа, не более двух раз в сутки;
- PostgreSQL;
- золото и журнал транзакций;
- инвентарь;
- магазин без рынка между игроками;
- сокращённая экипировка: оружие, броня, амулет, питомец;
- профессии;
- фракции;
- NPC: крестьянин и стражник;
- базовые административные команды;
- HTTP health-check для Render;
- модульная структура проекта.

## Важно о токене

Никогда не вставляйте токен в код и не загружайте его на GitHub. Создайте новый токен через BotFather и добавьте его в Render как секрет `BOT_TOKEN`.

## Развёртывание на Render

1. Распакуйте архив.
2. Создайте приватный репозиторий GitHub.
3. Загрузите в него содержимое папки проекта.
4. В Render откройте **New → Blueprint**.
5. Подключите репозиторий.
6. Render прочитает `render.yaml` и создаст:
   - Web Service;
   - PostgreSQL.
7. В переменных окружения укажите:
   - `BOT_TOKEN` — новый токен;
   - `ADMIN_IDS` — Telegram ID администраторов через запятую.
8. Нажмите Deploy.

`DATABASE_URL` подключается автоматически через Blueprint.

## Как узнать Telegram ID

Можно временно написать боту `@userinfobot` или другому безопасному ID-боту. Затем внесите число в `ADMIN_IDS`.

## Локальный запуск

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
cp .env.example .env
python -m app.main
```

Для Windows:

```powershell
python -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt
copy .env.example .env
python -m app.main
```

## Основные команды

- `/start`
- `/меню`
- `/профиль`
- `/персонаж`
- `/дом`
- `/сменить_портрет`
- `/сменить_фото_дома`
- `/тренировка`
- `/работа`
- `/работа_статус`
- `/инвентарь`
- `/экипировка`
- `/магазин`
- `/профессии`
- `/фракции`
- `/npc`
- `/помощь`
- `/admin`

## Административные команды

- `/admin`
- `/дать_золото TELEGRAM_ID AMOUNT`
- `/дать_опыт TELEGRAM_ID AMOUNT`
- `/запустить_событие ТЕКСТ`

## Особенности Render

Проект запускается как Web Service. FastAPI отвечает на `/` и `/health`, а Telegram-бот работает параллельно через long polling.

Render может перезапускать бесплатный сервис. Состояние не теряется, потому что:
- игровые данные находятся в PostgreSQL;
- изображения хранятся как Telegram `file_id`.

## Следующие модули

Архитектура подготовлена для дальнейшего добавления дуэлей, экспедиций, парламента, законопроектов, выборов и интерактивной карты.


## Версия 2
- Команды для групп: `/profile`, `/house`, `/shop`, `/ping`.
- Регистрация выполняется только в личном чате.
- `/admin` открывает панель назначения ролей.
- Магазин показывает 5 товаров и меняет их ежедневно.
- Для русских команд в группе отключите Privacy Mode в BotFather и заново добавьте бота.


## Мини-игры v3

### Дуэль
В группе ответьте `/duel` на сообщение соперника либо напишите `/duel @username`.
Действия: атака, магия, защита, сдача.

### Проклятый лабиринт
Введите `/expedition` в группе. До 6 игроков вступают кнопкой. Создатель запускает
пять этапов, на каждом все участники голосуют за маршрут. Награды начисляются всем.


## Обновление v4

### Улучшенные дуэли
В бою теперь реально работают все характеристики и экипировка:

- здоровье и выносливость увеличивают запас HP;
- мана и интеллект увеличивают запас маны;
- сила повышает физический урон;
- магия и интеллект усиливают заклинания;
- ловкость даёт преимущество 1–5% к уклонению и влияет на первый ход;
- выносливость даёт преимущество 1–5% к защите;
- удача даёт 1–5% дополнительного шанса критического удара;
- харизма может спасти персонажа при низком здоровье;
- уровень немного усиливает физический бой.

### Улучшенный Проклятый лабиринт
- общий запас здоровья рассчитывается из здоровья и выносливости игроков;
- общий запас маны зависит от маны и интеллекта;
- лес проверяет силу и ловкость;
- руины проверяют интеллект и удачу;
- портал проверяет магию, интеллект и общий запас маны;
- харизма повышает командную координацию;
- выносливость снижает получаемый командой урон;
- экипировка учитывается.

### Ежедневные нападения на дома
После часа `HOUSE_ATTACK_HOUR` каждый дом получает одну угрозу в сутки.

Игроку приходит личное уведомление. Есть 10 минут на выбор:
- сражаться лично;
- отправить стражников.

Если игрок не ответил:
- при наличии стражников они автоматически начинают бой;
- бой стражи длится 2–4 часа;
- без стражников дом сразу получает повреждение.

Личная победа даёт:
- +1 к силе или магии;
- 12–20 энергии ремонта;
- 15 XP.

Прочность дома и энергия ремонта открываются командой `/defense`.

### Панель создателя
Команда `/admin` позволяет:
- назначать роли;
- принудительно запускать тестовое нападение;
- полностью чинить все дома;
- смотреть статистику;
- запускать события.

### Amvera
В архив добавлен `amvera.yaml`. Секреты задаются в интерфейсе Amvera:
- `BOT_TOKEN`;
- `DATABASE_URL`;
- `ADMIN_IDS`;
- `TIMEZONE`;
- `HOUSE_ATTACK_HOUR`.


## Устойчивые таймеры Render

Версия v4.1 не полагается на непрерывный отсчёт в оперативной памяти.

Все сроки хранятся в PostgreSQL:
- `response_deadline` — окончание десятиминутного ожидания;
- `guard_finish_at` — окончание боя стражников;
- `last_attack_date` — дата последнего ежедневного нападения.

Добавлена защищённая HTTP-точка:

```text
/tasks/run?key=CRON_SECRET
```

При каждом вызове она:
1. создаёт положенные ежедневные нападения;
2. обрабатывает истёкшие 10 минут;
3. завершает готовые бои стражников;
4. отправляет уведомления игрокам.

### Переменная Render

Добавьте в Render:

```text
CRON_SECRET=длинная_случайная_строка
```

Например, создайте строку длиной не менее 32 символов. Не публикуйте её в GitHub.

### Настройка cron-job.org

1. Откройте сайт `cron-job.org`.
2. Создайте бесплатный аккаунт.
3. Нажмите **Create cronjob**.
4. В поле URL вставьте:

```text
https://ВАШ-СЕРВИС.onrender.com/tasks/run?key=ВАШ_CRON_SECRET
```

5. Поставьте выполнение каждые 5 минут.
6. Включите сохранение и уведомление об ошибках.

Первый запрос после сна Render может выполняться дольше из-за холодного запуска.
Следующий запуск обработает все сроки, поэтому игровые данные и награды не теряются.

### Проверка

Откройте в браузере:

```text
https://ВАШ-СЕРВИС.onrender.com/tasks/run?key=ВАШ_CRON_SECRET
```

При успехе вернётся JSON примерно такого вида:

```json
{"status":"ok","processed_at":"2026-07-11T12:00:00+00:00"}
```

Неверный ключ возвращает ошибку 403.
